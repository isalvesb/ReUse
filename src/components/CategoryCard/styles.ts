import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  card: {
    width: "48%",
  },

  image: {
    width: "100%",
    height: 120,
    borderRadius: 18,
  },
});

export default styles;
