import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  card: {
    width: 308,
  },

  title: {
    fontSize: 18,
    fontWeight: "700",
    color: "#342A2A",
  },

  subtitle: {
    marginTop: 4,
    fontSize: 14,
    fontWeight: "400",
    color: "#584C4C",
    lineHeight: 20,
  },

  image: {
    width: "100%",
    height: 156,
    marginTop: 12,
    borderRadius: 12,
  },
});

export default styles;
